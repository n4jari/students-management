import spinner from "./spinner.gif";

const Spinner = () => {
  return (
    <img
      src={spinner}
      style={{
        display: "block",
        margin:"1vw auto"
      }}
    />
  );
};

export default Spinner;
